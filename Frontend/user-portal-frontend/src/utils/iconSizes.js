/**
 * Uniform inline icon size — aligns ~with body-xs line-height (20px) from textStyles.
 */
export const ICON_SIZE = 20;

/** Side nav active-state circle (outer) */
export const NAV_ICON_PILL = 30;

/** Glyph inside the active nav pill */
export const NAV_ICON_PILL_INSET = 17;

/** Inactive nav / profile glyph footprint (width × height) */
export const NAV_ICON_OUTLINE = 20;
